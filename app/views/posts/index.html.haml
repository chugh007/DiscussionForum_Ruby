- @posts.each do |post|
  .each_post
    %h2.wrapper.title
      =link_to post.title,post
    %p.wrapper.post_date
      Published at
      =time_ago_in_words(post.created_at)
      by
      =post.user.email
